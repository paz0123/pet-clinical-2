git add .
git commit -m "Ton message"
git push origin main   

After downloading and opening the project in VS Code:

first type this to the terminal 
venv\Scripts\activate

then this for runing the web side
python app.py     
